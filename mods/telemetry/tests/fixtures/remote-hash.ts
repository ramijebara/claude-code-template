/**
 * The first sixteen hex digits of the SHA-256 of REPO's remote normalized
 * (`github.com/anthropics/claude-code`): what the rows carry as `rh`.
 */
export const REMOTE_HASH = 'd900cba35dcd6960'
