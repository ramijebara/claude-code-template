/**
 * Tried in order as the default branch when `refs/remotes/origin/HEAD` names
 * none that resolves; past them the guess is `main`, as the engine's is.
 */
export const CONVENTIONAL_DEFAULT_BRANCHES = ['main', 'master'] as const
