/**
 * The blank column the docked pane keeps at its right edge, as the built-in
 * sidebar's body does (its `paddingX`): rows wrap one column short of it.
 */
export const PANE_RIGHT_PAD_COLUMNS = 1
