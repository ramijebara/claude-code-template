/**
 * The terminal width from which `/diff` opens the pane (the built-in
 * sidebar's own minimum).
 *
 * The first edit opens it from this width too, when the person kept it
 * open before.
 */
export const OPEN_MIN_COLUMNS = 110
