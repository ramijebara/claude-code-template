/**
 * The most characters a prompt's context carries, its entries together; the
 * engine refuses a context past it, so an ask is fitted to what is left.
 */
export const PROMPT_CONTEXT_MAX_CHARS = 32_000
