/**
 * The most of a child's stdout `$.process.run` hands back, in bytes
 * (process-host OUTPUT_LIMIT); a listing that long was cut, silently.
 */
export const HOST_OUTPUT_CAP_BYTES = 4 * 1024 * 1024
