/**
 * What the header says instead of a count when there is nothing to list,
 * and the body's hint under it when the hint adds something.
 */
export type EmptyState = {
  headline: string
  hint: string | null
}
