/**
 * The inline pane's first line, as the built-in dialog heads itself: what
 * the rows are, then in dim what they are compared with.
 */
export type DialogTitle = {
  title: string
  subtitle: string
}
