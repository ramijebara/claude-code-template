/**
 * What the ring landing on a dialog row settles: the file now selected,
 * and the key of the row to land the ring on once the window re-centres.
 */
export type DialogFocus = {
  selectedPath: string
  landing: string
}
