/**
 * The one `$.command.register` refusal meaning the built-in `/diff` serves
 * this session, as the engine words it: the plugin stands down silently.
 */
export const BUILTIN_HOLDS_PATTERN = /refused: it is the built-in \//
