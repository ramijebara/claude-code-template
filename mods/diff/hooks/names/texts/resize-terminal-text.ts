/**
 * `/diff`'s answer on a terminal narrower than the built-in panel shows
 * on, worded as the built-in words it.
 *
 * Also the pane's one line where the surface seats it inline (no room
 * beside the transcript).
 */
export const RESIZE_TERMINAL_TEXT =
  'Resize your terminal to at least 110 columns to show the diff panel'
