/**
 * The most nodes the drawn file bodies hold between them, a hunk's `Code`
 * one, a file's frame FILE_FRAME_NODES.
 *
 * Under the host's whole-tree cap (2 000), with room for the list, the
 * pre-session legend and the pane's rest.
 */
export const MAX_BODY_NODES = 1_500
