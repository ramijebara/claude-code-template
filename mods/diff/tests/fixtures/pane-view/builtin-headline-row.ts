/**
 * The panel row the built-in draws its empty headline on, docked in a
 * 40-row terminal over one pre-session file.
 *
 * Its header's top pad, the close, the header's bottom pad, then half the
 * spare rows.
 */
export const BUILTIN_HEADLINE_ROW = 17
