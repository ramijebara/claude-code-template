/**
 * The row the first context of a load sends under `claude-md-or-agents-md` and
 * `claude-md-and-agents-md`: what was handed on, in counts.
 *
 * The engine's own initial-load row counts its CLAUDE.md files the same way.
 */
export const LOAD_EVENT = 'agents_md_load'
